package com.test.library1;

import android.os.Bundle;
import android.widget.ProgressBar;

public class MovieTest extends BaseActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_movie_test);

//        ProgressBar progressBar = findViewById(R.id.progress_bar);
//        progressBar.setIndeterminateDrawable(getResources().getDrawable(com.test.library1.R.drawable.ic_wifi_timing));

    }
}
